import { Box, TextField, styled } from '@mui/material';

const StyledInput = styled(TextField)`
  display: none;
`;
const ImageContainer = styled(Box)`
  margin-bottom: 20px;
`;

export { StyledInput, ImageContainer };
